Clone the repo git clone https://github.com/abhinavmishra094/emurgo.git

Run command npm install

Create env file using envtemplate

Install nodemon using command npm i -g nodemon

Run using commang npm run dev 
